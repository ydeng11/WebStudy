import React from 'react';

const Index: () => React.JSX.Element = () => {
    return (
        <div className="bg-cover bg-center h-64 flex items-center justify-center" style={{ backgroundImage: 'url(/background.jpg)', backgroundSize: 'cover'}}>
        </div>
    );
};

export default Index;
